[build]
builder = "DOCKERFILE"
watchPatterns = [
  "Dockerfile",
  ".dockerignore",
  "railway.toml",
  "elixir/**"
]

[deploy]
healthcheckPath = "/api/v1/kepler/health"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
overlapSeconds = 0
drainingSeconds = 30
